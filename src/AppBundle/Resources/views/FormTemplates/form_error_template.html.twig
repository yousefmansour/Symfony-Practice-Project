{%- block form_errors -%}
    {% spaceless %}
        {%- if errors|length > 0 -%}
            <i>{{ errors|first.message }}</i>
        {%- endif -%}
    {% endspaceless %}
{%- endblock form_errors -%}